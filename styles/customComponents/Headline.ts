import styled from "styled-components";

export const Headline = styled.h1`
	position: relative;
	font-family: var(--first-font);
	color: var(--col-3);
	text-align: center;
	font-size: 3rem;
	z-index: 4rem;
`;
